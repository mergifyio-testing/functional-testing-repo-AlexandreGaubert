queue_rules:
  - name: default
    batch_size: 2
    batch_max_wait_time: "10 seconds"
    queue_conditions:
      - label = queue
    merge_conditions:
      - check-success = unit-tests

pull_request_rules:
  - name: queue on label
    conditions:
      - base = main
      - label = queue
    actions:
      queue:
